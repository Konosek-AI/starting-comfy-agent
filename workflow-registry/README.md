# Workflow Registry

Each reusable production workflow should record:
- id
- name
- status
- path
- purpose
- capabilities
- model family
- required resources
- editable parameters
- advanced parameters
- fixed pipeline
- last verification time

The registry is an index, not the workflow itself.
