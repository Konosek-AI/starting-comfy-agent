# Troubleshooting Skill

`actual error → responsible subsystem → exact cause → smallest fix → validate → execute → verify`

Evidence priority:
1. actual runtime error
2. live node schema
3. actual resource inventory
4. execution result
5. current workflow state
6. cached notes

Do not reinstall everything first or repeat unchanged executions.
If the same approach fails twice without new evidence, reassess the diagnosis.
